server.port=8081
server.servlet.context-path=/api

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/tesis_parkinson}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:giomar}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:giomar2003@}

spring.datasource.hikari.maximum-pool-size=${SPRING_DATASOURCE_POOL_MAX:10}
spring.datasource.hikari.minimum-idle=${SPRING_DATASOURCE_POOL_MIN:2}

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never

jwt.secret=${JWT_SECRET:prod-secret-256-bit-change-in-production-min-32-chars}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

app.minio.endpoint=${MINIO_ENDPOINT:http://localhost:9000}
app.minio.public-endpoint=${MINIO_PUBLIC_ENDPOINT:https://minio.rakiweb.com}
app.minio.access-key=${MINIO_ACCESS_KEY:giomar}
app.minio.secret-key=${MINIO_SECRET_KEY:giomar2003@}

app.n8n.webhook-url=${N8N_WEBHOOK_URL:http://localhost:5678/webhook/parkinson/analyze}
app.webhook.recording-secret=${WEBHOOK_RECORDING_SECRET:parkinson-n8n-webhook-prod-2026}

app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://parkinson.rakiweb.com,http://localhost:5173}
